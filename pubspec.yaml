name: windows_gpu_recovery
description: >
  Flutter Windows plugin that recovers from EGL_CONTEXT_LOST / D3D11 device
  removed (DXGI_ERROR_DEVICE_REMOVED) and LiveKernelEvent 141 GPU failures.
  Uses native Windows detection and a Vectored Exception Handler to safely
  destroy the dead engine and create a fresh one.
version: 0.1.1

topics:
  - windows
  - gpu
  - d3d11
  - recovery
  - plugin

repository: https://github.com/DitriXNew/windows_gpu_recovery
issue_tracker: https://github.com/DitriXNew/windows_gpu_recovery/issues

environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: '>=3.27.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  plugin:
    platforms:
      windows:
        pluginClass: WindowsGpuRecoveryPluginCApi
